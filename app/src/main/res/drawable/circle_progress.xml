<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Borde exterior blanquecino -->
    <item>
        <shape
            android:shape="ring"
            android:thickness="14dp"
            android:useLevel="false">
            <solid android:color="#44EFEFEF" />
        </shape>
    </item>

    <!-- Fondo del círculo (Carril) -->
    <item>
        <shape
            android:shape="ring"
            android:thickness="10dp"
            android:useLevel="false">
            <solid android:color="#22282828" />
        </shape>
    </item>

    <!-- Progreso principal -->
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270">
            <shape
                android:shape="ring"
                android:thickness="10dp"
                android:useLevel="true">
                <solid android:color="?attr/colorPrimary" />
                <!-- Borde INTERNO y EXTERNO usando stroke -->
                <stroke android:width="1dp" android:color="#CCFFFFFF"/>
            </shape>
        </rotate>
    </item>

    <!-- Efecto de brillo interno (Aro muy fino dentro) -->
    <item android:top="1dp" android:bottom="1dp" android:left="1dp" android:right="1dp">
        <shape
            android:shape="ring"
            android:thickness="1dp"
            android:useLevel="false">
            <solid android:color="#30FFFFFF" />
        </shape>
    </item>
</layer-list>